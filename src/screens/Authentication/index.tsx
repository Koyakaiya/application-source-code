/**
 * this index.tsx was initially used as the stack navigator index.
 * refactored. the stack is now called AuthStack available in the
 * stack folder of the project
 */

import { SplashScreen } from './SplashScreen';
//
/**
 * Export splash screen from index
 */
export default SplashScreen;
